with "gtkada.gpr";

project get_intersection_edges is

	--for exec_dir use external ("HOME") & "/bin";
	for exec_dir use ".";
	for object_dir use "obj";
 	for source_dirs use ("." , "../../lib");
 	for main use ("get_intersection_edges");

end get_intersection_edges;

-- build with command "gprbuild -j4 get_intersection_edges.gpr"
-- build with command "gprbuild -j2 -g"
-- build with command "gprbuild -j4 -gnatm4"
-- clean up with command "gprclean"
