with "gtkada.gpr";

project rounding is

	--for exec_dir use external ("HOME") & "/bin";
	for exec_dir use ".";
	for object_dir use "obj";
 	for source_dirs use ("." , "../../lib");
 	for main use ("rounding");

end rounding;

-- build with command "gprbuild"
-- build with command "gprbuild -j4 -gnatm4"
-- clean up with command "gprclean"
